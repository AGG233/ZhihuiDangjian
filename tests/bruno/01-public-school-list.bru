meta {
  name: 01 Public School List
  type: http
  seq: 1
}

get {
  url: __BASE_URL__/api/school/all
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("school list responds successfully", function () {
    expect(res.getStatus()).to.equal(200);
    const body = res.getBody();
    expect(body.code).to.equal("200");
    expect(body.data).to.not.equal(undefined);
  });
}
