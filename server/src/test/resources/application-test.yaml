spring:
  application:
    name: server
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 100ms
  ai:
    openai:
      base-url: https://api.deepseek.com
      api-key: dummy-key
      chat:
        options:
          model: deepseek-chat

auth:
  captcha:
    test-code: TEST8888

dromara:
  x-file-storage:
    tencent-cos:
      - platform: tencent-cos
        enable-storage: false
        secret-id: test
        secret-key: test
        region: ap-guangzhou
        bucket: test-bucket
        domain: http://localhost
        base-path: /
